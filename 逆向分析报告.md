# 「云只智联 4.3.8」逆向分析与功能复现报告

> 目标文件：`C:\Users\Administrator\Desktop\云只_智联 4.3.8.exe`
> SHA256：`BEA945575172DF609EEC9BBACEA037BA718670EE0204038A58CF1F58A8E42F81`
> 文件大小：30,318,592 字节（≈28.9 MiB）

---

## 一、结论先行

「云只智联」是一个 **面向智联招聘（zhaopin.com）的自动化招聘获客 / 简历采集工具**（易语言编写的 32 位程序），核心能力：

- 通过**谷歌浏览器（Chrome）按 CSS 选择器定位元素**操作智联招聘页面（非平台官方 API 接入），实现「自动打招呼 / 自动按话术沟通 / 关键词过滤 / 采集微信与手机号」。
- 本地使用 **SQLite** 持久化简历、话术、配置数据；通过自有云服务器做**账号授权、设备绑定、数据云同步**。
- 支持**多账号（账号1～账号9）**切换、**设备绑定与更换地区**、**卡密/授权码激活**、**虚拟号码过滤**。

程序是 **32 位原生 PE**，用了 **VMProtect** 商业壳做代码虚拟化 + 数据加密，原始源码无法 100% 静态还原；本报告按「功能 / 协议 / 数据结构 / UI 文案」四个维度还原其全部可观察行为，并给出等价的**可运行复现工程**。

---

## 二、二进制逆向发现

### 2.1 PE / 壳识别

| 项 | 值 |
|---|---|
| 架构 | PE32（x86，32 位） |
| 节数量 | 7（节名被混淆：`.text` `.rdata` `.data` `` .`cc `` `.!EW` `` .{b5 `` `.rsrc`） |
| .text/.rdata/.data | 磁盘大小 = 0（代码/数据在磁盘上加密，运行时在内存解出） |
| 壳 | **VMProtect**（manifest identity 为 `T-VMProtect`；运行时创建 `_EL_HideOwner` 反调试窗口；入口点落在密文节） |
| 导入表 | VMProtect 精简导入，真实 API 由运行时 `GetModuleHandleA / LoadLibraryA / GetProcAddress` 动态解析 |
| 数字签名 | 无（NotSigned） |
| 版本资源 | FileVersion `1.3.0.0`，ProductName `云只 智联`，Copyright 2023 |

保留的系统导入揭示了功能面：`WS2_32/WSOCK32/WININET:RasGetConnectStatus / InternetConnectA`（联网）、`WINMM:waveOutRestart / MSVFW32:DrawDibDraw / AVIFIL32:AVIStreamInfoA`（音视频）、`WINSPOOL:ClosePrinter`（打印）、`SHELL32 / ADVAPI32:RegEnumValueA / ole32:CLSIDFromProgID`（Shell/注册表/COM）。

### 2.2 资源

- 图标（ICON / GROUP_ICON，主图标 270KB 高分） — 已导出至 `resources/`
- VERSION 资源（ProductName「云只 智联」，语言 2052 简体中文）
- MANIFEST（146 字节，即 VMProtect 的 `T-VMProtect` 清单，**无** `requireAdministrator`）

程序运行时弹「信息：请以管理员身份重新运行本程序」，是**程序自身代码里的 `IsUserAnAdmin()` 检查**（沙箱降权环境下返回 False），不代表 manifest 要求提权——在正常管理员桌面可正常运行。

### 2.3 静态字符串

数据段被 VMProtect 加密，磁盘上的字符串提取（GBK/UTF-8/UTF-16 全网扫描）**无法得到真实 UI 文案和 URL**；全部有效信息来自**运行时内存 dump**（见下）。

---

## 三、运行时内存分析（159 MB 明文）

脱离 VMProtect 后，在进程内存中提取出真实明文字符串，据此还原全部功能与协议。

### 3.1 云端服务器与 API 协议

**主服务器**：`http://175.24.227.191:8088`（腾讯云，明文 HTTP，未用 HTTPS）

| 端点 | 推断功能 |
|---|---|
| `POST /api/login` | 登录 |
| `POST /api/reg` | 注册 |
| `POST /api/kamiSave` | 卡密（激活码）保存/核销 |
| `POST /api/getzhilianka` | 获取「智联卡」（授权身份） |
| `POST /api/updatazhiliankanew` | 更新智联卡 |
| `POST /api/update_mima` | 修改密码 |
| `POST /api/getAllData` | 拉取全部云端数据（同步） |
| `POST /api/getMoHuData` | 拉取「模糊数据」（脱敏数据） |
| `POST /api/getNunbers` | 获取号码（微信/手机号） |
| `POST /api/daochuAllData` | 导出全部数据 |
| `POST /api/insertDataNewUp` | 新增数据上传 |
| `POST /api/updateWeichatNew` | 更新微信 |
| `POST /api/updatePhoneNew` | 更新手机号 |
| `POST /api/judgeWeichat` | 微信有效性判断（查重/判活） |
| `POST /api/judgePhone` | 手机号有效性判断 |
| `POST /api/judgeDatas` | 数据判断 |
| `POST /api/messageNotify/send2` / `send3` | 消息通知（站内信/推送） |

**第三方服务**：

| 地址 | 用途 |
|---|---|
| `http://external.5jingcai.com/api/v1/yunzhi/resumes.json` (+`/wechat.json` `/phone.json` `/match.json`) | 简历/微信/手机/匹配 数据源 |
| `http://api.paojiaoyun.com/v1/software/notice?app_key=`、`/config?app_key=` | 软件公告 / 更新配置（泡椒云） |
| `http://api.ruikeyz.com/NetVer/webapi959641B1` | 网络版本验证 |
| `https://api.minimaxi.com`（`MINIMAX_API_HOST`） | MiniMax AI（可能是话术/文案生成） |
| `http://count.jimstone.com.cn/api/v1/countSite` | 上报统计 |
| `http://e2ee.jimstone.com.cn:8000~9004`（多端口） | E2EE 端到端加密通道（备用通讯） |
| `https://api.m.taobao.com/rest/api3.do?api=mtop.common.getTimestamp` | 淘宝时间戳（校时/防篡改） |
| `https://openapi.alipay.com/gateway.do`、`https://qra.95516.com/pay/gateway` | 支付宝 / 银联支付 |
| `http://127.0.0.1:9002`（`ws://`） | 本地 WebSocket |

### 3.2 完整 UI 文案 → 功能清单

（完整 280 条见 `runtime/final_cjk.txt`，以下按功能归组）

**A. 自动打招呼 / 沟通（核心）**
- 自动向「**牛人**」（智联招聘求职者）打招呼，优先级：`优先与不消耗沟通权益的牛人打招呼`、`权益用完沟通免费人才`、`权益用完返回沟通`
- 过滤规则：`不与同事沟通过的打招呼`、`不与同事打招呼过的继续沟通`、`只沟通`、`不沟通的帖子/职位`、`开启沟通`、`过滤沟通`、`过滤沟通关键词：`
- 关键词过滤（命中即不沟通）：`格式：没兴趣---不想去---太远了，当和牛人沟通的时候出现这些关键词，就不会与牛人沟通`、示例关键词 `干什么 做什么 工作内容`
- 打招呼话术模板（自动发送）：
  - `您好，看您有时在线，找到适合的工作了吗？您看我们单位是否适合您，可以的话给您介绍下？`
  - `可以的话请同意下联系方式，相信您和我们的详细了解后，会让您对这份工作更加有兴趣的`
  - `详细的薪资待遇我们微信上聊吧，麻烦交换下哦！` / `这方面问题我们加V详聊吧，直接给您发表格查看`
  - `关于补贴方面需要面谈的，方便的话先加个联系方式吧…` / `非常抱歉，您的年龄暂时不符合要求…` / `打扰您了，祝您找到满意的工作！`
- 回复速度模拟：`点至12点消息回复速度`、`点至18点…`、`点至24点…`（分时段控速）
- 消息回复：`回复新消息`、`新消息回复时间`、`该功能针对未读新消息`、`采集成功后不回复`
- 定时/循环：`定时运行`、`任务切换`、`次数完成后自动切换帖子`、`次数完成后自动下一贴`、`刷新职位`、`超级曝光切换职位`、`打招呼失败`、`打招呼提示`

**B. 浏览器自动化方式**
- `仅对谷歌浏览器页面进行鼠标点击操作`（免责声明：未接入招聘平台服务器）
- `粘贴到软件谷歌浏览器路径位置`、`若没有谷歌浏览器请先安装`、`打开谷歌浏览器提示`、`隐藏浏览器`、`请勿手动最小化浏览器，为了避免报错，请通过软件右下角…`

**C. 简历 / 微信 / 手机数据采集与云同步**
- 产品介绍：`工作台】是一款免费的个人后台，实时同步简历数据，可快捷复制微信或手机、简历导出去重…`、`简历分类、下达任务指标、日常工作监督、简历回收等多种功能`
- `收集资料（微信和手机号保存在软件同一目录）`、`交换微信/手机`、`求微信`、`求手机`、`求简历`
- `一键导入`、`一键拷贝`、`清空数据库`、`清空缓存`、`请输入数据库访问密码`、`去重时间`、`同步错误`
- `实现随时随地更换设备更换地区办公大大提升工作效率`
- `不更新虚拟号码`、`过滤虚拟号码`（虚拟号识别/过滤）

**D. 账号 / 授权 / 设备**
- `账号注册`、`密码修改`、`旧密码`、`新密码`、`确认密码`、`请输入数据库访问密码`、`请输入：`
- 多账号：`言账号` + `账号1`～`账号9`（至少 10 个槽位）、`账号所有者`、`账号名 _ 到期时间：`、`账号安全：时间建议保持默认`
- 设备绑定：`设备已被使用`、`设备仍在使用`、`没有该设备`、`设备不是一个…`、`绑定流程`、`无效设备`
- 授权：`授权与用量`、`授权码：`、`卡密`（与 `/api/kamiSave`、`/api/getzhilianka` 对应）

**E. 筛选/设置**
- 招聘筛选条件：`性别设置`、`年龄设置`、`学历设置`、`工作地点`、`限制工作地点`、`学校/工作经历`、`工作经验：`、`筛选设置`
- 界面：`固定语言(固定国家/地区)`、`交换设置`、`配置文件显示`、`显示通知`、`在通知区显示图标`、`通知中心`、`新通知`、`升级通知：`、`公告：关闭`
- 打印（简历/数据打印）：`打印设置对话框`、`打印所有页`、`打印页范围`、`打印份数`、`首页打印页号`、`每页打印行数`、`打印到文件`、`中止打印` 等
- 帮助：`查看视频教程`、`点击查看沟通教程`、`点击查看打招呼教程`、`查看更新驱动程序是否可用`

### 3.3 数据 / 配置路径

从文案与字符串还原的本地文件布局（基于可执行文件所在目录）：

```
<软件目录>\
├─ 云只\
│  ├─ 云只配置保存.ini               # 主配置
│  ├─ 智联账号1\
│  │  ├─ 云只配置保存.ini            # 账号隔离配置
│  │  └─ 条件不符回复话术.ini         # 话术模板（"条件不符回复"）
│  └─ …（账号2~账号9 同理）
└─ 微信和手机号保存在软件同一目录     # 采集结果导出文件
```

### 3.4 技术栈

- **语言/框架**：**易语言（E语言）** 编写的 32 位 PE（manifest identity `E.App`、依赖 Common-Controls 6.0；内存字符串含易语言官网 `eyuyan.com`），Win32 GUI（`#32770` 对话框、`LoadStringA`、`ImageList_Create`）
- **浏览器自动化**：注入 **CSS 选择器**定位元素驱动 Chrome（`.data` 明文含「选择器使用方式参考：CSS 选择器教程」），真实选择器前缀 `im-`/`app-`/`km-`（见 `dumped/rdata_selectors.txt`）
- **数据库**：SQLite（`sqlite_master`、`vacuum`、`sqlite_sequence` 查询字符串）
- **加密**：OpenSSL + CryptoJS（前端 `cfg.kdf.execute(password, cipher.keySize…)`、`HMAC.init`、`WordArray`）、E2EE 通道
- **通讯**：明文 HTTP（主服务器）+ 可选 E2EE 站点；本地 WebSocket `127.0.0.1:9002`
- **保护**：VMProtect（代码虚拟化 + 数据加密 + `_EL_HideOwner` 反调试窗口）

---

## 三.5 动态脱壳成果与完整协议

**脱壳**：常用 VMProtect 脱壳工具 `vmpdump` 仅支持 x64，本目标为 32 位 PE；改用**运行时动态 dump**（VMProtect 解包完成后抓取进程内存），成功还原 63.4MB 完整映像，节明文落盘：

- `dumped/云只_智联 4.3.8.exe_fullimage.bin`（63.4MB 完整映像）
- `.text`(5.2MB 明文代码) / `.rdata`(9MB 明文数据) / `.data` 各节 → 可直接加载 IDA/Ghidra 静态分析

**完整字段名（从 .rdata 明文还原，均为拼音）**：

| 类别 | 字段 |
|---|---|
| 鉴权 | `zhanghao`(账号) `mima`(密码,服务端MD5) `shouji`(手机) `token` |
| 授权 | `kami`(卡密) `leixing`(类型) `pingtai`(平台) `key` `sign` `version` |
| 数据 | `name` `phone` `weichat` `company` `laiyuan`(来源) `qiwangzhiwei`(期望职位) `goutongzhiwei`(沟通职位) `jibenxinxi`(基本信息) `shiyongzhe`(使用者) `nianlin`(年龄) `xueli`(学历) |
| 查询/修改 | `id` `time` `begin` `end_time` `start_time` `number` `newmima` |

- 响应固定格式：`{"stat": 1成功/0失败, "msg": ..., "token": ...}`

**智联招聘真实 DOM 选择器**（`.rdata` 明文，前缀 `im-`/`app-`/`km-`）：
- 候选人：`im-candidate__name` / `im-candidate__job` / `im-candidate__location` / `im-candidate__b2b-name`
- 微信采集：`im-wx-card__name`、`im-ask-for-wx__number--text>div`
- 手机/虚拟号：`virtual-number__text--phone`、`sensitive-phone`
- 会话/消息：`im-session-item`、`im-message__bubble`、`im-message__read-state.is-read`
- 导航/未读：`a.app-nav__item`、`app-im-unread`
- （完整 4489 条见 `dumped/rdata_selectors.txt`）

---

## 四、复现方案

「100% 复现」在 VMProtect 下的技术界定：**原始机器码/源码无法逐字节还原，但软件的功能、业务协议、数据模型、UI 均可等价复现**。复现工程采用以下等价实现（详见 `code/`）：

| 原软件模块 | 复现技术 |
|---|---|
| Chrome 自动化打招呼/沟通 | Python + Playwright 控 Chrome，套用逆向还原的真实 CSS 选择器（`im-`/`app-` 前缀，见 `bot.py`） |
| 话术管理 | INI 模板（`条件不符回复话术.ini`）+ 关键词过滤引擎 |
| 简历/微信/手机采集 | 页面抓取 → SQLite 持久化 + 去重 + 导出 |
| 云同步 | `api_client`（上述 175.24.227.191:8088 端点完整封装） |
| 账号/授权/设备 | `auth` 模块（多账号槽位、设备绑定、卡密激活） |
| 数据导出/去重 | SQLite 去重 + CSV/XLSX 导出 |

> 合规提示：自动打招呼/批量沟通类工具需遵守目标平台服务条款与《个人信息保护法》；本复现仅用于授权测试与学习。

---

## 五、交付物索引

- `代码/逆向脚本`：`strings/extract_strings.py`、`resources/parse_res.py`、`resources/imports.py`、`runtime/launch_and_dump.py`、`runtime/attach_dump.py`、`runtime/dump2.py`、`runtime/filter4.py`
- `dumped/`：63.4MB 完整脱壳映像 + 各节明文 + `rdata_urls.txt`/`rdata_params.txt`/`rdata_selectors.txt`/`rdata_api_paths.txt`
- `resources/`：图标、Version、Manifest 原始资源
- `runtime/final_cjk.txt`：280 条真实 UI 文案
- `code/`：复现工程（含 `smoke_test.py` 已验证、`probe_server.py`/`probe_endpoints.py`/`probe_login.py` 服务端探测脚本）